import React from 'react';

import Header from '@components/Header';

const App: React.FC = () => (
  <div className='App'>
    <header className='App-header'>
      <Header />
    </header>
  </div>
);

export default App;
